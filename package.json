{
  "name": "mathy",
  "private": true,
  "version": "0.0.0",
  "description": "Juego para aprender matemática de cero a nivel universitario",
  "license": "MIT",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "validate:docs": "python3 tools/validate.py"
  },
  "devDependencies": {
    "@types/node": "^22.20.2",
    "typescript": "^5.9.2"
  },
  "allowScripts": {
    "@shopify/react-native-skia@2.6.2": true
  }
}
